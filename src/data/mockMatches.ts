// Legacy placeholder retained for compatibility with prior commits.
// Real match data now comes from football-data.org via /api/matches endpoints.
export const mockMatches = [];
export const allTeams = [];
