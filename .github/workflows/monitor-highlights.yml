name: Monitor PL highlights

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  monitor-pl:
    runs-on: ubuntu-latest
    steps:
      - name: Monitor PL
        run: curl -fsS "https://footyam-v1.vercel.app/api/cron-monitor?league=PL"
